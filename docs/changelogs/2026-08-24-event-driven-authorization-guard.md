# 改动说明

## 元信息

| 字段 | 值 |
|------|-----|
| 日期 | 2026-08-24 |
| 作者 | AI 助手 |
| 分支 | 当前工作区 |
| 起始 commit | working-tree |
| 结束 commit | working-tree |
| 关联需求/单号 | 事件驱动沉淀与文件执行门禁首版接入 |

## 功能改动点

- 将“事件驱动沉淀 + 权限配置接入执行链路”的实施要求同步补充到 3 份主文档。
- 新增 `Authorization Guard` 服务端门禁模块，统一解析桌面端已保存的权限配置。
- 首批将目录权限规则接入 `src/routes/api/files.ts`，覆盖目录浏览、读取、下载、写入、重命名、删除、上传、创建目录。
- 目录规则命中时，返回明确的错误码与提示，不再以黑箱方式处理。
- 文件执行链路的放行 / 拒绝结果统一发布为事件，为后续审计与习惯沉淀打底。
- 顺手修正 `files.ts` 中目录树相对路径在 profile/workspace 场景下的根目录计算问题。

## 涉及文件列表

- `docs/Ti 系列多端统一改造研发实施蓝图.md`
- `docs/Ti 系列多端统一改造完整方案..md`
- `docs/Ti Work 企业日常办公平台规划.md`
- `src/server/authorization-guard.ts`
- `src/routes/api/files.ts`
- `src/test/authorization-guard.test.ts`

## 影响到的功能模块

- 权限与安全
- Authorization Guard
- 文件执行链路
- 事件驱动沉淀
- 单元测试

## 测试要点（AI 生成脚本时按此展开）

- 验证 `scoped / allowlist / observe` 三种目录策略行为正确。
- 验证禁止目录优先级高于允许目录。
- 验证文件执行链路在命中禁止规则时返回明确错误码。
- 验证门禁结果会转成统一事件，为后续习惯沉淀提供来源。

## 风险点 / 遗留事项

- 当前首批只把目录权限正式接入文件执行链路，站点权限与高风险动作审批仍需继续接入浏览器、终端与其他本地适配器。
- 事件已经开始统一化，但云端画像提炼、审批流和更细粒度的策略缓存还属于后续阶段。
